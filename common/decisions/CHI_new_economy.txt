CHI_new_social_economy_decisions = {
	ruins_rebuild = {
		icon = GFX_decision_generic_construction
		activation = {
				NOT = { has_global_flag = Ccw }
				any_owned_state = { has_dynamic_modifier = { modifier = CW_ruins } }
		}
		available = { 
			custom_trigger_tooltip = {
				tooltip =  ruins_rebuild_finish_trigger_tt
				all_owned_state = { NOT = { has_dynamic_modifier = { modifier = CW_ruins } } }
				#对于state范围，all是用于判定范围，any用于效果添加
			}
		}
		cancel_trigger = {
			always = no
		}
		complete_effect = {
			add_stability = 0.10
			add_offsite_building = { type = industrial_complex level = 3 }
		}
		selectable_mission = no
		days_mission_timeout = 90
		is_good = yes
		highlight_states = { highlight_states_trigger = { has_dynamic_modifier = { modifier = CW_ruins } } }
		timeout_effect = {
			random_owned_state = {
				limit = { has_dynamic_modifier = { modifier = CW_ruins } }
				remove_dynamic_modifier = { modifier = CW_ruins }
			}
		}
		ai_will_do = {
			factor = 1
		}
	}

	Redemption_assets = {
		icon = GFX_decision_gre_investment_decisions
		visible = {
			has_completed_focus = CHI_redemption
			check_variable = { Redemption_assets_times < 4 }
		}
		available = {
			custom_trigger_tooltip = {
				tooltip = Redemption_assets_trigger_tt
				check_variable = { Redemption_assets_times < 4 }
			}
		}
		custom_cost_trigger = {
			check_variable = { treasury > 10.0 }
		}
		custom_cost_text = cost_10_0
		complete_effect = {
			set_temp_variable = { treasury_change = -10 }
			modify_treasury_effect = yes
		}
		days_remove = 20
		remove_effect = {
			random_owned_state = {
				if = {
					limit = {
						free_building_slots = {
							building = offices
							size > 0
							include_locked = no
						}
					}
					add_building_construction = {
						type = offices
						level = 1
						instant_build = yes
					}
				}
				else_if = {
					limit = {
						free_building_slots = {
							building = offices
							size = 0
							include_locked = no
						}
					}
					add_extra_state_shared_building_slots = 1
					add_building_construction = {
						type = offices
						level = 1
						instant_build = yes
					}
				}
			}

			hidden_effect = {
				if = {
					limit = { NOT = { has_variable = Redemption_assets_times } }
					set_variable = {
						var = Redemption_assets_times
						value = 1
					}
				}
				else = {
					add_to_variable = {
						var = Redemption_assets_times
						value = 1
					}
				}
			}
		}
		ai_will_do = {
			factor = 1
		}
	}

	confiscated_assets = {
		icon = GFX_decision_gre_investment_decisions
		visible = {
			has_completed_focus = CHI_redemption
			check_variable = { confiscated_assets_times < 6 }
		}
		available = {
			custom_trigger_tooltip = {
				tooltip = confiscated_assets_trigger_tt
				check_variable = { confiscated_assets_times < 6 }
			}
		}
		cost = 50
		complete_effect = {
			add_stability = -0.02
		}
		days_remove = 20
		remove_effect = {
			modify_timed_idea = {
				idea = CHI_confiscated
				days = 20
			}
			random_owned_state = {
				if = {
					limit = {
						free_building_slots = {
							building = industrial_complex
							size > 1
							include_locked = no
						}
					}
					add_building_construction = {
						type = industrial_complex
						level = 2
						instant_build = yes
					}
				}
				else_if = {
					limit = {
						free_building_slots = {
							building = industrial_complex
							size = 0
							include_locked = no
						}
					}
					add_extra_state_shared_building_slots = 2
					add_building_construction = {
						type = industrial_complex
						level = 2
						instant_build = yes
					}
				}
			}

			hidden_effect = {
				if = {
					limit = { NOT = { has_variable = confiscated_assets_times } }
					set_variable = { confiscated_assets_times = 1 }
				}
				else = {
					add_to_variable = { confiscated_assets_times = 1 }
				}
			}
		}
		ai_will_do = {
			factor = 1
		}
	}

}
#国企决议，只显示在地图上
CHI_state_enterprises_decisions = {
	guangdong_state_enterprises = {
		icon = GFX_decision_generic_construction
		available = { 
			has_completed_focus = CHI_Organize_state_enterprises
		}
		cost = 35
		complete_effect = {
			
		}
		highlight_states = { highlight_states_trigger = { state = 534 state = 532 } }
		on_map_mode = map_only
		remove_effect = {
			state_enterprises_build_effect = yes
		}
		ai_will_do = {
			factor = 1
		}
	}


}