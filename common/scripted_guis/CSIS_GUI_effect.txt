scripted_gui = {

	CISC_main_GUI = {
		context_type = decision_context
		window_name = "CISC_main_GUI_window"

		visible = {
			always = yes
		}

		effects = {
			XIN_button_click = {
				if = {
					NOT = { has_country_flag = XIN_CSIC_GUI_OPENED }
				}
				set_country_flag = XIN_CSIC_GUI_OPENED
			}

			
		}
	}

	

}
